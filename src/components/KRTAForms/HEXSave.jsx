

const HEXSave = ({values, HEX_Calc}) => {
  return (
    values.grossWeight = HEX_Calc.grossWeight,
    values.attachments.bucket_exca_capa = HEX_Calc.bucket_exca_capa,

    ''
    
  )
}

export default HEXSave